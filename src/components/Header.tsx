type Props = {
}

export const Header = ({ }: Props) => (
	<header style={{ gridArea: 'header', textAlign: 'center', borderBottom: '4px solid currentcolor' }}>
		<h1>PDF Template Generator</h1>
	</header>
)
